import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'truth-or-dare-frontend',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
