describe('Sets', () => {
    beforeEach(() => {
        cy.getSets()
        cy.visit('/')
    })

    it('should list all sets on explore page', () => {

    })

    it('should show loading bar when load sets and hide when loading finished on explore page')

    it('should show name of set on detail page')

    it('should show creator of set on detail page')

    it('should show correct task count of set on detail page')

    it('should show all tasks of set on detail page')

    it('should show loading bar when load tasks and hide when loading finished on detail page')

    it('should show modal when click on task on detail page')

    it('should show toast when click on three dots in nav on detail page')
})

export { }

